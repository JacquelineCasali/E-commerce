<%- include("./partials/head") %>

<input type="checkbox" id="sidebar-toggle">
<div class="sidebar">
  <div class="sidebar-header">
      <h3 class="brand">
          <span>Approve</span>
      </h3>
      <label for="sidebar-toggle" class="ti-menu-alt"></label>
  </div>

  <div class="sidebar-menu">
      <ul>
          <li>
              <a href="/admin-produtos">
                  <span class="ti-home"></span>
                  <span>Tela Inicial</span>
              </a>
          </li>
          <li>
              <a href="/admin-produtos">
                  <span class="ti-face-smile"></span>
                  <span>Vendedores</span>
              </a>
          </li>
          <li>
              <a href="/admin-produtos">
                  <span class="ti-clipboard"></span>
                  <span>Pagamentos</span>
              </a>
          </li>
          <li>
              <a href="/admin-produtos">
                  <span class="ti-book"></span>
                  <span>Produtos</span>
              </a>
          </li>
          <li>
              <a href="/admin-produtos">
                  <span class="ti-settings"></span>
                  <span>Config. e Relatórios</span>
              </a>
          </li>
      </ul>
  </div>
</div>
    <main class="main-content">
      <header></header>
        <h1>Criar produto</h1>
        <% if(locals.error) { %>
            <h2 class="error-message"><%= error.message %></h2>
            <br />
            <% } %>
        <form action="/admin-produtos/criar" method="post" enctype="multipart/form-data">
          <input type="text" name="name" placeholder="Nome do produto" 
            <% if (locals.text.name) { %>
                value="<%= text.name %>"
            <% } %>
          />
          <br />
          <input type="text" name="size" placeholder="Tamanho" 
            <% if (locals.text.size) { %>
                value="<%= text.size %>"
            <% } %>
          />
          <br />
          <input type="text" name="department" placeholder="Categoria" 
            <% if (locals.text.department) { %>
                value="<%= text.department %>"
            <% } %>
          />
          <br />
          <input type="number" name="price" placeholder="Preço" pattern="[0-9]+([,\.][0-9]+)?" min="0" step="any" 
            <% if (locals.text.price) { %>
                value="<%= text.price %>"
            <% } %>
          />
          <br />
          <input type="text" name="description" placeholder="Descrição" 
            <% if (locals.text.description) { %>
                value="<%= text.description %>"
            <% } %>
          />
          <br />
          <input type="number" name="inventory" placeholder="Inventário" pattern="[0-9]+([,\.][0-9]+)?" min="0" step="any" 
            <% if (locals.text.inventory) { %>
                value="<%= text.inventory %>"
            <% } %>
          />
          <br />
          <input type="number" name="rating" placeholder="Classificação" pattern="[0-9]+([,\.][0-9]+)?" min="0" step="any" 
            <% if (locals.text.rating) { %>
                value="<%= text.rating %>"
            <% } %>
          />
          <br />
          
          <select name="status">
            <% if (locals.text.status === "Ativo") { %>
                <option value="" ></option>
                <option value="Ativo" selected>Ativo</option>
                <option value="Inativo">Inativo</option>
            <% } else if (locals.text.status === "Inativo") { %>
                <option value="" ></option>
                <option value="Ativo" >Ativo</option>
                <option value="Inativo" selected>Inativo</option>
            <% } else { %>
                <option value="" selected></option>
                <option value="Ativo">Ativo</option>
                <option value="Inativo">Inativo</option>
            <% } %>
          </select>
          <br /> 
          <label for="produto-img" style="align-self: baseline;">Imagem do Produto</label>
          <input type="file" id="produto-img" name="produto-img" accept="image/png, image/jpeg">
          <br />
          <br /> 
          <br /> 
          <div class="form-buttons">
            <a href="/admin-produtos" class="back-button"><button class="reset-button" type="button">Cancelar</button></a>
            <button class="add-button" type="submit">Adicionar produto</button>
          </div>
        </form>
        
    </main>
<%- include("./partials/footer") %>